import { EternalPercent } from '../ArrayList/StepPerList';

function EternalBonusStatsCalc({ value, key_ }) {

  const per = EternalPercent[value];

  return(
    <></>
  )
}

export default EternalBonusStatsCalc;